= link_to request.portal.short_name, request, :class => "label request #{(logged_in? and current_user.voted_requests.exists?(request)) ? "voted" : ""}", :style => "background-color:##{request.portal.color}", :target => "_blank"